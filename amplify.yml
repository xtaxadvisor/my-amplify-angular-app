version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - ng build --configuration production
  artifacts:
    baseDirectory: dist/amplify-angular-template
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .npm
      